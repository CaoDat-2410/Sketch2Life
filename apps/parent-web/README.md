# Parent/guide mode boundary (inactive in mobile-only MVP)

The mobile-only plan places Gate A, Gate B, history, and guide/reviewer flows inside `apps/mobile/`. This folder is retained only as a historical reference to the handbook's original web split and must not receive new product code.
