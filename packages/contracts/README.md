# Contracts package boundary

Versioned JSON Schema/OpenAPI contracts and generated client types. Python Pydantic models belong at the backend boundary; TypeScript types are generated into `generated/typescript/`. No infrastructure imports and no hand-maintained duplicate mobile schemas.
