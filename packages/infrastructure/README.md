# Infrastructure adapter boundary

Concrete database, object-storage, queue, provider, and telemetry adapters live here. Infrastructure implements application ports and translates external failures into explicit application outcomes.
