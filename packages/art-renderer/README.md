# Original-art renderer boundary

This package owns the PixiJS + GSAP WebView renderer boundary. The skeleton currently exports only the versioned protocol; rendering behavior is added under an approved art-player feature and must preserve original child assets.
