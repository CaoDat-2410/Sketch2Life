wan:
  provider: mock
  model_name: mock
  python_binary: python
  inference_script: /tmp/mock-generate.py
  checkpoint_dir: /tmp/mock-model
  output_dir: /tmp/mock-output
  offload_model: true
  convert_model_dtype: true
  t5_cpu: true

video:
  min_duration_sec: 5
  max_duration_sec: 10
  width: 320
  height: 176
  fps: 8

runtime:
  max_retries: 1
