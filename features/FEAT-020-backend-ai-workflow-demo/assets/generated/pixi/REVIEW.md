# PixiJS Asset Review

**Status:** `GENERATED_PENDING_REVIEW`
**Origin:** hand-authored SVG/vector shapes; no image-generation model was used.
**Scope:** repository/catalog only; PixiJS is not implemented by FEAT-020.

The assets in this directory are candidates for later visual approval. Do not reference them from a production/mobile renderer until the visual review is approved and the selected files are copied into the governed `approved/` and `applied/` locations.

Review checklist:

- [ ] child-art identity is never replaced by decorative assets;
- [ ] color/contrast works on a light paper-like background;
- [ ] SVG viewBox and dimensions are valid;
- [ ] asset ID/path/version match `ASSET_CATALOG.json`;
- [ ] no external URL, font, or runtime network dependency;
- [ ] no generative asset call is needed during playback;
- [ ] all activity/safety symbols are understandable to a caregiver.
