# FEAT-020 Plan Navigation and Precedence

The canonical owner-confirmed input decision is recorded in:

1. `INPUT_BOOTSTRAP_CORRECTION.md`
2. `INPUT_BOOTSTRAP_FINAL.md`

Those files supersede the earlier exploratory `INPUT_BOOTSTRAP_ADDENDUM.md` and `INPUT_BOOTSTRAP_DECISIONS.md` proposal for runtime image/audio generation. The implemented workflow must use pre-generated, committed, replaceable `test-assets/input-image.png` and `test-assets/narration.wav`; it must not generate them on every run.
