# FEAT-020 Context Addendum — Owner Confirmations

**Date:** 2026-09-12

- The unattended gate actor is `DEMO_OPERATOR`.
- The committed input pair is the default E2E input: synthetic image plus Vietnamese narration WAV.
- The first demo stops after all backend context/handoff records are connected. Video generation is deferred and must be represented as `VIDEO_DEFERRED`, never as a fabricated success.
- PixiJS is not implemented here. This feature now includes a reviewed-pending, hand-authored SVG asset catalog and a stable asset-selection contract for a future renderer.
