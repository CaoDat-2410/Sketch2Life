# FEAT-013 phase gates

Current state: `PHASE_4_OWNER_REVIEW`.

## Phase 0 - approval and baseline freeze

Exit requires revision 1 approval, selected 20 IDs accepted, 55-73 hour estimate acknowledged, and FEAT-002 hashes captured. No implementation may run before exit.

## Phase 1 - v2 contract and semantics

Tasks: G1-01 through G1-03.

Exit requires schema examples, field guide, readiness/material/progression rules, baseline immutability check, and architecture review.

## Phase 2 - four age-band content batches

Tasks: G1-04 through G1-07.

Each batch exits independently after five full records pass schema/content checks and receive a review sheet. No batch may weaken hard rules or claim production approval.

## Phase 3 - harness and fixtures

Task: G1-08.

Exit requires 60+ passing fixtures, coverage metrics, deterministic rerun, deliberate expected-output failure, baseline-mutation failure, and no-network execution.

## Phase 4 - owner review and handoff

Tasks: G1-09 and G1-10.

Exit requires owner decision for every golden record, traceability, architecture/team/security checks, known limitations, and clean feature-local evidence. Final state may become `DONE` while remaining provisional/non-production.

Automated implementation, traceability, architecture/team/security checks, and handoff evidence are complete. Per-record owner decisions remain pending, so this phase has not exited.
