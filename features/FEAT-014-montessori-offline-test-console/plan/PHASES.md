# FEAT-014 phase gates

Current state: `PHASE_1_SHARED_EVALUATOR_PARITY`.

## Phase 0 - approval and input freeze

Exit requires explicit FEAT-014 revision 1 approval, separate approval and passing implementation of the FEAT-013 portability correction, canonical FEAT-013 input hashes, and acceptance of the activity-explicit/no-ranking boundary.

## Phase 1 - shared evaluator parity

Exit requires a pure evaluator, catalog loader, and exact parity across all 74 FEAT-013 fixtures without content or reason-order drift.

## Phase 2 - console adapters

Exit requires list/evaluate/replay/guided modes, stable text/JSON, documented exit codes, and no automatic activity selection.

## Phase 3 - evidence and negative security

Exit requires feature-confined opt-in evidence, traversal/absolute/overwrite rejection, malformed-input checks, and fixture-only warnings.

## Phase 4 - review and handoff

Exit requires examples, full tests, architecture/team/security/harness gates, traceability, limitations, and owner review of the console behavior.
