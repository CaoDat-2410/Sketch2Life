# FEAT-014 approval packet

## Decision requested

Two linked decisions are requested:

1. Approve or reject FEAT-013 portability corrective revision 1.
2. Approve or reject FEAT-014 plan revision 1 for an offline Montessori test console owned by Person 1.

Approval authorizes only:

- one explicitly selected activity per evaluation;
- local deterministic Golden v2 inputs and reason codes;
- list, scriptable replay, and guided prompt adapters;
- opt-in sanitized evidence under FEAT-014;
- shared evaluator refactor with exact 74-fixture parity;
- 12-16 hours / 3-4 story points.

Approval does not authorize recommendation/ranking, backend/mobile/AI integration, real child data, external services, Golden content changes, or production use.

Reply `approve both revisions` to authorize the narrowly scoped FEAT-013 integrity correction followed by FEAT-014 revision 1. You may also approve/reject either item separately. Because two decisions are pending, a generic `approve` will require interpreting both only if it directly follows this exact packet.
